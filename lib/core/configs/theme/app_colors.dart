import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xff8E6CEF);
  static const background = Color(0xff1D182A); // Used in login/create account
  static const dashboardBackground = Color(0xFF0A0E21); // Used in HomePage
  static const secondBackground = Color(0xff342F3F);
  static const card = Color(0xFF1D1E33);
  static const accent = Color(0xFFEB1555);
  static const text = Colors.white;
}
